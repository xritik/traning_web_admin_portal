import loginImage from './img/login.jpg';

const LoginPage = () => {
  return (
    <div
      className="LoginPage"
      style={{ backgroundImage: `url(${loginImage})` }}
    >
      {/* Your login form */}
    </div>
  );
};

export default LoginPage;
